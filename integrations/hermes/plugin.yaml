name: basic-memory
version: 0.23.2
description: "Basic Memory — persistent knowledge graph backed by the basic-memory MCP server"
manifest_version: 2
api_version: 1
kind: exclusive
python_dependencies:
  - "mcp>=2,<3"
hooks:
  - prefetch
  - queue_prefetch
  - sync_turn
  - on_session_end
  - shutdown
